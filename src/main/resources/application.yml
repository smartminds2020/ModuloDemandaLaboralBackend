spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: UPG.FISI@unmsm.edu.pe
    password: tuclave
    
    
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  
  datasource:
    driverClassName: org.postgresql.Driver
    url: jdbc:postgresql://161.35.60.197/tcs7
    username: modulo4
    password: modulo4

server:
   port: 8080
   
eureka:
  client:
    serviceUrl:
      defaultZone: https://modulo-alumno-eureka.herokuapp.com/eureka #http://localhost:8761/eureka/
  instance:
    hostname: localhost
    nonSecurePort: 8080

banner:
  location: classpath:/example/banner.txt
 
spring.mvc.contentnegotiation.media-types.xlsx : application/vnd.openxmlformats-officedocument.spreadsheetml.sheet

